# Copy this file to .env.local and fill in your values
ANTHROPIC_API_KEY=sk-ant-...

# Jira credentials are provided by the user in the browser (localStorage)
# and sent per-request — they are never stored server-side
